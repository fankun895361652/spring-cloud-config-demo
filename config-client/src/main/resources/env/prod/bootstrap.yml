spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://127.0.0.1:9501
      profile: prod

management:
  security:
    enabled: false
endpoints:
  refresh:
    enabled: true
