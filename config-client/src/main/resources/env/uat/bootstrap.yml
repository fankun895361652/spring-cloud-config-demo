spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://127.0.0.1:9601
      profile: uat

management:
  security:
    enabled: false
endpoints:
  refresh:
    enabled: true
