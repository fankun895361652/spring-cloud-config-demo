spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://127.0.0.1:9401
      profile: dev

management:
  security:
    enabled: false
endpoints:
  refresh:
    enabled: true
